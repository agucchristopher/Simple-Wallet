import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Line = () => {
  return (
    <View style={{ width: "100%", height: 1.8, backgroundColor: "grey" }} />
  );
};

export default Line;

const styles = StyleSheet.create({});
